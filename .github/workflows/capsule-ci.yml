name: Intelligent Capsule CI

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validation will be re-enabled after all implementation files are published."
