name: Guardei CI

on:
  push:
    branches: [main, "feat/**", "fix/**"]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    name: Build and validate
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            server/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Install backend dependencies
        run: npm install --prefix server

      - name: Generate Prisma client
        run: npm --prefix server run db:generate
        env:
          DATABASE_URL: postgresql://user:password@localhost:5432/guardei
          DATABASE_DIRECT_URL: postgresql://user:password@localhost:5432/guardei

      - name: Check backend syntax
        run: |
          node --check server/src/index.js
          node --check server/src/routes/pushRoutes.js
          node --check server/src/push/webPush.js
          node --check server/scripts/generate-vapid.js

      - name: Run smart product tests
        run: npm test

      - name: Build frontend
        run: npm run build
